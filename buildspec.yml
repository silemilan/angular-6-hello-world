version: 0.2

#env:
  #variables:
     # key: "value"
     # key: "value"
  #parameter-store:
     # key: "value"
     # key: "value"
  #git-credential-helper: yes

phases:
  install:
    #If you use the Ubuntu standard image 2.0 or later, you must specify runtime-versions.
    #If you specify runtime-versions and use an image other than Ubuntu standard image 2.0, the build fails.
    #runtime-versions:
      # name: version
      # name: version
    commands:
    - nmp install -g @angular/cli
      # - command
  #pre_build:
    #commands:
      # - command
      # - command
  build:
    commands:
    - npm install 
    - ng build --prod 
      # - command
  #post_build:
    #commands:
    - aws s3 cp dist s3://s3bucket-angular-test/dev-test --recursive
      # - command
      # - command
artifacts:
  files:
  - 'target/dist'
    # - location
    # - location
  #name: $(date +%Y-%m-%d)
  #discard-paths: yes
  #base-directory: location
#cache:
  #paths:
    # - paths
